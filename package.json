{
  "name": "spider-league-sdi",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-data.mjs",
    "check": "npm run validate && npm test && node --check app.js && node --check core.js && node --check league.js && node --check league-core.js && node --check admin.js && node --check firebase-client.js && node --check firebase-config.js && node --check service-worker.js"
  },
  "engines": {
    "node": ">=22"
  }
}
